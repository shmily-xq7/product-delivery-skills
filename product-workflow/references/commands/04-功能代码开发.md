任务：根据 `输入` 内容要求，生成或者更新项目中的相关代码工程。

输入：用户输入的任务描述
页面详细设计文档：`项目战术执行` 目录下相关页面的详细设计文档，根据用户输入的内容确定详细设计文档名称

流程：
  1. 深度分析用户输入内容的需求、目标页面、功能点等
  2. 根据确定的目标页面，深度分析 `项目战术执行` 目录下相关 `页面详细设计文档` 内容，确定页面结构、功能点、数据流等
  3. 根据分析结果，拟定高质量的解决方案
  4. 执行解决方案

要求：
  - 遵守技术栈基线（单一事实源见 skill `product-development` 的「技术栈基线」）：
    前端 React 18 + TypeScript + Vite + React Router v6 + **Ant Design 主线**（不引入 Tailwind / 第二套组件库）；状态管理 Zustand / Redux Toolkit
    后端 FastAPI + SQLAlchemy + PostgreSQL
  - 命名：对外 API 字段 camelCase；DB 列 snake_case；前端组件与页面文件 PascalCase；样式文件 kebab-case
  - 枚举禁止前端硬编码，须由后端集中定义并下发
  - 视觉 token 取 skill `product-ui-spec`

notion:
- deep think and plan a detailed todo list and do it step by step.
